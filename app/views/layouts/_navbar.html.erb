
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <p class="navbar-brand navbar-left"><%= image_tag("logo.png", height: '25px') %> </p>
          <a class="navbar-brand navbar-left" href="/">LIC Soccer</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <% if logged_in? %>
            <li><%= link_to("Create Event", new_event_path) %> </li>

            <li><%= link_to("All Events ⚽", events_path) %>
            <% end %>
      
         
          </ul>
             <ul class="nav navbar-nav navbar-right">
                <% if logged_in? %>
                    <li> <%= link_to("Logout", '/logout') %> </li>
                    <li> <p class="navbar-text"> You are logged in as: <%= current_player.username %></p></li>
                <% else %>
                    <li> <%= link_to("Sign Up", new_player_path) %></li>
                    <li>  <%= link_to("Log In", '/login') %> </li>
                    <li> <%= link_to("Log In With Google", '/auth/google_oauth2') %></li>
                <% end %>
            </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>
<nav>
   