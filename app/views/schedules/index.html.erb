

<% @schedules.each do |e| %>
  <div>
    <h3><%= e.name%> <%= link_to "Create new #{c.name}", new_category_shoe_path(c)  %></h3>
    <ul>
    <% c.shoes.each do |shoe| %>
       <li><%= link_to(shoe.brand_and_color, shoe_path(shoe.id))  %></li>
    <% end %>
    </ul>
  </div>
<% end %>