<% @schedules.each do |s| %>
    <div>
      <h3><%= s.name%> <%= link_to "Create new #{s.name}", new_schedule_event_path(c)  %></h3>
      <ul>
      <% s.events.each do |event| %>
         <li><%= link_to(events. , event_path(event.id))  %></li>
      <% end %>
      </ul>
    </div>
  <% end %>